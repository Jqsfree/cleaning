# .archive — 过期文件归档

| 目录 | 内容 |
|------|------|
| `superseded/` | 旧入口：`phase2b_vision_qc`、`qc_vision_standalone`、`chunk_text_qc_v2`/`*_beauty`、`reports.py` 等 |
| `junk/` | 误生成的空文件、临时日志 |
| `bak/` | QC/清洗过程中的 `*.bak_*`（含 `film_tv/` 等，按原相对路径） |
| `docs/` | 过期计划文档（如目录规范化计划） |

实验脚本：`experiments/`（ML filter、`welding_sb/` 等）。
